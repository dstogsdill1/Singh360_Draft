Continue from the CURRENT Singh360_Draft repo state. Do not redo the tooltip repair.

1. Inspect the live branch, HEAD, upstream, Git status, staged diff, unstaged diff, and latest failure transcript.
2. Preserve all valid current source work. Never reset, stash, clean, force-push, switch branches, delete .docs, or commit .docs.
3. Clean the working tree safely: no generated logs, backups, temp files, workbooks, PDFs, images, frontend/dist, or node_modules may be staged.
4. Diagnose the exact remaining browser failure: `.workspace-drawing-tabs [role=tab]` never became ready.
5. Capture DOM, URL, console/page errors, failed requests, API responses, and a screenshot before editing.
6. Fix the APP if the Drawing Pages strip is missing or broken. Fix the TEST only if the strip is present and the selector/readiness logic is stale. Do not merely increase the timeout.
7. Use disposable SINGH360_DOCS_DIR and copied workbooks. Never run SAVE + WRITE EXCEL on live 829 or SA31.
8. Run:
   - python -m compileall -q core server.py
   - python -m pytest -q tests/test_sync_order_reconciliation.py tests/test_project_workspace.py tests/test_workbook_authority.py
   - cd frontend && npm run build
   - cd .. && python scripts/smoke_sync_order_workspace.py
   - git diff --check
9. Recheck 829, SA31, both linked workbooks, and the component library are unchanged.
10. When every test passes, commit a NEW commit: `fix: render and verify Data Workspace drawing tabs`
11. Push the current branch, verify remote SHA equals local HEAD, restart Singh360 Draft, verify health, and open 829 read-only.
12. Report root cause, files changed, backup path, exact test results, commit SHA, pushed branch, and anything not tested.

Do not ask me to diagnose files or stitch commands together. Finish it end to end.