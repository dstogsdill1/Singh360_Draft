# Singh360 Draft — 11x17 Engineering Document Publishing Engine
pandas>=2.0
openpyxl>=3.1
flask>=3.0,<4

# PDF export via server.py /api/export/pdf/<id>
# After installing: playwright install chromium
playwright>=1.40

# PDF component conversion (library PDF -> rendered PNG component)
pymupdf>=1.24
pikepdf>=10.11
python-pptx>=0.6.23
requests>=2.31
Pillow>=10.0
CairoSVG>=2.7

# S360 SYMBOL MAPPER START
# Review-only visual template matching; deterministic text/vector matching still applies.
numpy>=1.26
opencv-python-headless>=4.10
# S360 SYMBOL MAPPER END
