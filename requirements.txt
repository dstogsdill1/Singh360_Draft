# Core relational engine (required)
pandas>=2.0
openpyxl>=3.1   # read raw MEP schedule .xlsx workbooks (--schedule-xlsx)

# Web bridge (web/index.html GUI <-> main_generator.py). Required only to run
# the local server (python server.py); the CLI pipeline runs without it.
flask>=3.0,<4

# Optional — live Azure Document Intelligence spatial ingestion path only.
# The deterministic CSV/VSON/VSDX pipeline runs without these.
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0
PyMuPDF>=1.24.0
python-dotenv>=1.0.0
