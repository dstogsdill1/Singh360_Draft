# Public GitHub Pages files

This folder is only the public Singh360 Pages site.

- `index.html` redirects to the component catalog.
- `component-library/` contains published active components.

Local projects and the editable source library live under `.docs/` and are
intentionally excluded from Git.
